# Docker Environment Configuration
# Copy this file to .env for Docker deployment
# This file contains Docker-specific variables not in environment.example

# Database Configuration (Docker)
POSTGRES_DB=fortexa
POSTGRES_USER=fortexa
POSTGRES_PASSWORD=fortexa123
POSTGRES_PORT=5432

# Redis Configuration (Docker)
REDIS_PORT=6379

# API Configuration (Docker)
API_PORT=8000
BUILD_TARGET=production

# Monitoring (Docker)
FLOWER_PORT=5555
FLOWER_USER=admin
FLOWER_PASSWORD=admin

# Nginx (Docker - for production)
NGINX_PORT=80
NGINX_SSL_PORT=443

# Development Override (Docker)
# Uncomment for development
# DEBUG=true
# BUILD_TARGET=development

# Note: For other variables, copy them from environment.example
# This file only contains Docker-specific variables 

# Important: Make sure to set FRONTEND_URL in your main .env file
# FRONTEND_URL=https://fortexa.tech 